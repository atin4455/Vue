# 📝 ASP.NET Core MVC + Vue 3 (ESM Mode) Todo List

這是一個練習專案，在 ASP.NET Core 環境中實現 Vue 3 的開發。

## 📅 明日待辦清單 (To-Do for Tomorrow)

### 第一階段：前端互動功能
- [ ] 實作 `v-model` 與 `v-for` 渲染。

### 第二階段：後端整合
- [ ] 使用 `fetch` 或 `axios` 將資料持久化至 C# 後端。
- [ ] 實作讀取/存入本地資料庫 (SQL Server / SQLite)。
---
